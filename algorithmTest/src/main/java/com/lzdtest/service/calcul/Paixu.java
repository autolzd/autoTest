package com.lzdtest.service.calcul;

public class Paixu {
    /**
     * 输入整型数组和排序标识，对其元素按照升序或降序进行排序
     * 1≤n≤1000  ，元素大小满足
     * 0≤val≤100000
     * 输入描述：
     * 第一行输入数组元素个数
     * 第二行输入待排序的数组，每个数用空格隔开
     * 第三行输入一个整数0或1。0代表升序排序，1代表降序排序
     */
    //num 数组大小 str 待排序数组 order 排序规则
    private static String[] paix(String num,String str,String order){
        String[] strings = new String[Integer.parseInt(num)];
        for(int i = 0 ;i < strings.length;i++){
            int strs = Integer.parseInt(strings[i]);
            
        }

        return strings;
    }
}
